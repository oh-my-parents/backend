spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/omp?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1q2w3e4r1!
  jpa:
    hibernate:
      ddl-auto: create #create? ?? ?????? ?? ??? ?? ??? ?? ???? ? ?? ??? ??? ??? ?? ???? ???? ??? ?
    properties:
      hibernate:
        # show_sql: true #?? org.hivernate.SQL? ???? ?? System.out?? ?? ?? ??? ??? ???. ?? ??? ? ??
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

logging: #?? ??? ??? ?
  level:
    org.hibernate.SQL: debug #JPA ??????? ???? SQL? ? ???
    org.hibernate.type: trace